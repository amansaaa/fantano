[project]
name = "fantano-pipeline"
version = "0.1.0"
description = "Offline data pipeline: YouTube -> captions -> AI extraction -> MySQL"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pymysql>=1.2.3",
    "python-dotenv>=1.2.3",
    "youtube-transcript-api>=1.2.4",
]
